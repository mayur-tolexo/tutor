---
id: cbse-11/swap-two-numbers
title: "Swap two numbers"
concepts: [variables, assignment, tuple-unpacking]
syllabus: "11.2.2"
difficulty: 1
---

Read two integers `a` and `b`. Swap their values and print them.

**Input:** two lines — `a`, then `b`.

**Output:** one line: the swapped values separated by a space (so `b` first, then `a`).
