---
id: cbse-11/tuple-max-min
title: "Largest and smallest in a tuple"
concepts: [tuple, tuple-traversal]
syllabus: "11.6.1"
difficulty: 1
---

Read `n` numbers into a **tuple** and print the largest and smallest by traversing it (not with `max()`/`min()`).

**Input:** first line `n`; second line `n` integers.

**Output:** two lines:
```
Largest: <x>
Smallest: <y>
```
