---
id: cbse-11/division-results
title: "Quotient, remainder and division"
concepts: [integer-division, modulo, float-division]
syllabus: "11.2.4"
difficulty: 1
---

Read two positive integers `a` and `b` and print, on three separate lines: the integer quotient `a // b`, the remainder `a % b`, and the true division `a / b`.

**Input:** two lines — `a`, then `b`.

**Output:** three lines.
